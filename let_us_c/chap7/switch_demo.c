/* Switch demo */
/* The control statement that allows us to make a decision from the number
 * of choices is called a switch, or more correctly a switch-case-default,
 * since these three keywords go together to make up the control statement.
 */

switch (integer expression) {
    case constant 1:
        do this;
    case constant 2:
        do this;
    case constant 3:
        do this;
    default:
        do this;
}
